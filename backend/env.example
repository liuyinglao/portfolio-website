NODE_ENV=development
PORT=5001

# CORS Configuration
# For development (default): http://localhost:3000
# For production: Set to your actual frontend URL(s), comma-separated
# ALLOWED_ORIGINS=https://myapp.vercel.app,https://myapp.com

# For production deployment, add:
# NODE_ENV=production
# ALLOWED_ORIGINS=https://your-actual-frontend-domain.com 